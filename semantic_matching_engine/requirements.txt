numpy>=1.26
scikit-learn>=1.3
pytest>=7.0

# Optional (real semantic embeddings; falls back to offline hashing engine
# automatically if unavailable or the model can't be downloaded)
# sentence-transformers>=3.0
